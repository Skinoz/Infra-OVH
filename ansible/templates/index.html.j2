<!DOCTYPE html>
<html>
<head>
    <title>Web Server</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }
        .container {
            text-align: center;
            background: white;
            padding: 50px;
            border-radius: 10px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.2);
        }
        h1 { color: #333; margin: 0 0 20px 0; }
        p { color: #666; font-size: 18px; }
        .info {
            margin-top: 20px;
            padding: 10px;
            background: #f5f5f5;
            border-radius: 5px;
        }
        button {
            margin: 10px;
            padding: 10px 20px;
            font-size: 16px;
            cursor: pointer;
            background: #667eea;
            color: white;
            border: none;
            border-radius: 5px;
        }
        button:hover { background: #5568d3; }
        #result {
            margin-top: 20px;
            padding: 10px;
            background: #e8f4f8;
            border-radius: 5px;
            max-height: 300px;
            overflow-y: auto;
        }
        input {
            margin: 10px;
            padding: 10px;
            font-size: 16px;
            width: 300px;
        }
        .data-list {
            margin-top: 20px;
            text-align: left;
        }
        .data-item {
            background: #f0f0f0;
            padding: 10px;
            margin: 5px 0;
            border-radius: 5px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .delete-btn {
            background: #ff4444;
            padding: 5px 10px;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <div class='container'>
        <h1>🚀 Web Server Frontend</h1>
        <p>Serveur web Nginx avec proxy vers API Backend + Database</p>
        <div class='info'>
            <p><strong>Version:</strong> {{ server_version }}</p>
            <p><strong>Configuré par:</strong> Ansible</p>
        </div>
        <div>
            <button onclick="testAPI()">Test API</button>
            <button onclick="getAPIData()">Get Data</button>
            <button onclick="getAPIStatus()">API Status</button>
        </div>
        <div>
            <input type="text" id="newValue" placeholder="Nouvelle valeur...">
            <button onclick="addData()">Ajouter Donnée</button>
        </div>
        <div id="result"></div>
    </div>
    
    <script>
        async function testAPI() {
            try {
                const response = await fetch('/api/info');
                const data = await response.json();
                document.getElementById('result').innerHTML = '<pre>' + JSON.stringify(data, null, 2) + '</pre>';
            } catch (error) {
                document.getElementById('result').innerHTML = '<p style="color:red;">Error: ' + error.message + '</p>';
            }
        }
        
        async function getAPIData() {
            try {
                const response = await fetch('/api/data');
                const data = await response.json();
                
                let html = '<div class="data-list">';
                html += '<h3>Données (' + data.count + ' entrées)</h3>';
                
                data.data.forEach(item => {
                    html += '<div class="data-item">';
                    html += '<span><strong>#' + item.id + '</strong>: ' + item.value + ' <em>(' + new Date(item.created_at).toLocaleString() + ')</em></span>';
                    html += '<button class="delete-btn" onclick="deleteData(' + item.id + ')">Supprimer</button>';
                    html += '</div>';
                });
                
                html += '</div>';
                html += '<p><em>Source: ' + data.hostname + '</em></p>';
                
                document.getElementById('result').innerHTML = html;
            } catch (error) {
                document.getElementById('result').innerHTML = '<p style="color:red;">Error: ' + error.message + '</p>';
            }
        }
        
        async function addData() {
            const value = document.getElementById('newValue').value;
            if (!value) {
                alert('Veuillez entrer une valeur');
                return;
            }
            
            try {
                const response = await fetch('/api/data', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ value: value })
                });
                
                const data = await response.json();
                document.getElementById('newValue').value = '';
                document.getElementById('result').innerHTML = '<pre>' + JSON.stringify(data, null, 2) + '</pre>';
                
                // Recharger les données après ajout
                setTimeout(getAPIData, 1000);
            } catch (error) {
                document.getElementById('result').innerHTML = '<p style="color:red;">Error: ' + error.message + '</p>';
            }
        }
        
        async function deleteData(id) {
            if (!confirm('Supprimer cette donnée ?')) return;
            
            try {
                const response = await fetch('/api/data/' + id, {
                    method: 'DELETE'
                });
                
                const data = await response.json();
                document.getElementById('result').innerHTML = '<pre>' + JSON.stringify(data, null, 2) + '</pre>';
                
                // Recharger les données après suppression
                setTimeout(getAPIData, 1000);
            } catch (error) {
                document.getElementById('result').innerHTML = '<p style="color:red;">Error: ' + error.message + '</p>';
            }
        }
        
        async function getAPIStatus() {
            try {
                const response = await fetch('/api/status');
                const data = await response.json();
                document.getElementById('result').innerHTML = '<pre>' + JSON.stringify(data, null, 2) + '</pre>';
            } catch (error) {
                document.getElementById('result').innerHTML = '<p style="color:red;">Error: ' + error.message + '</p>';
            }
        }
    </script>
</body>
</html>
